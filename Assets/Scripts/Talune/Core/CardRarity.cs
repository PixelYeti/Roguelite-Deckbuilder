namespace Talune.Core
{
    /// <summary>Fix 5's rarity tiers (Hybrid is a Type, not a rarity - see CardType).</summary>
    public enum CardRarity
    {
        Common,
        Uncommon,
        Rare,
    }
}
